module.exports = {
  // Use the @tailwindcss/postcss plugin (the Tailwind PostCSS plugin moved to this package).
  // We keep autoprefixer as well.
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
}
